-- folke/which-key.nvim: the original author's own actively-maintained
-- successor to liuchengxu/vim-which-key. Picks up the `desc` fields you
-- see on the keymaps in lua/config/keymaps.lua and lua/plugins/*.lua
-- automatically, so there's no separate g:which_key_map table to keep
-- in sync anymore.
return {
  "folke/which-key.nvim",
  event = "VeryLazy",
  opts = {},
}
